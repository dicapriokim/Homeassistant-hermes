#!/usr/bin/env bash
set -Eeuo pipefail

if [[ -r /run/hermes-ha/runtime.env ]]; then
  # shellcheck disable=SC1091
  . /run/hermes-ha/runtime.env
fi

exec /usr/bin/node /usr/local/share/hermes-ha/ha-memory.mjs "$@"

