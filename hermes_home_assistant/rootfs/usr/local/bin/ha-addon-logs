#!/usr/bin/env bash
set -Eeuo pipefail

if (( $# != 1 )) || [[ ! "$1" =~ ^[a-z0-9_-]+$ ]]; then
  printf 'Usage: ha-addon-logs ADDON_SLUG\n' >&2
  exit 64
fi

exec supervisor-api --raw --accept text/x-log GET "/addons/$1/logs"
